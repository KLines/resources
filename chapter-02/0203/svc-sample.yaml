kind: Service
apiVersion: v1
metadata:
  name: svcname-nginx
spec:
  selector:
    app: sample
  type: NodePort
  ports:
  - name: svcport-nginx
    protocol: TCP
    port: 8080
    targetPort: 80
    nodePort: 30881
