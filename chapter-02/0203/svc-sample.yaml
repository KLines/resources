# kubectl port-forward --address 0.0.0.0 svc/svcname-nginx 8080:8080
kind: Service
apiVersion: v1
metadata:
  name: svcname-nginx
spec:
  selector:
    app: sample
  ports:
  - name: svcport-nginx
    protocol: TCP
    port: 8080
    targetPort: 80
